import Link from "next/link";
import type { Campaign } from "@/services/campaign.service";

export default function CampaignSection({ campaigns }: { campaigns: Campaign[] }) {
  if (!campaigns.length) return null;
  return <section className="mx-auto w-full max-w-[1280px] px-4"><div className="space-y-8">{campaigns.map((campaign) => <div key={campaign.id} className="overflow-hidden rounded-2xl border border-orange-100 bg-gradient-to-r from-orange-50 to-amber-50"><div className="relative p-5 sm:p-7">{campaign.banner_image && <img src={campaign.banner_image} alt="" className="absolute inset-0 h-full w-full object-cover opacity-20" />}<div className="relative mb-5 flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wider text-orange-600">Limited-time offer</p><h2 className="text-2xl font-bold text-gray-900">{campaign.name}</h2>{campaign.description && <p className="mt-1 text-sm text-gray-600">{campaign.description}</p>}</div><Link href={`/campaigns/${campaign.slug}`} className="rounded-lg bg-orange-500 px-4 py-2 text-sm font-semibold text-white">{campaign.button_text}</Link></div><div className="relative grid grid-cols-2 gap-3 sm:grid-cols-4">{campaign.products.slice(0,4).map((product) => <Link key={product.id} href={`/product/${product.slug}`} className="rounded-xl bg-white p-3 shadow-sm hover:shadow-md"><div className="aspect-square overflow-hidden rounded-lg bg-gray-100">{product.main_image && <img src={product.main_image} alt={product.name} className="h-full w-full object-cover" />}</div><p className="mt-2 line-clamp-1 text-sm font-medium text-gray-900">{product.name}</p><p className="text-sm font-bold text-orange-600">৳{product.price.toLocaleString("en-BD")}</p><p className="text-xs text-gray-400 line-through">৳{product.original_price.toLocaleString("en-BD")}</p></Link>)}</div></div></div>)}</div></section>;
}
